Apache

# Całkowita blokada dostępu z zewnątrz do plików biblioteki
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>